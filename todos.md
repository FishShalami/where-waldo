**To-Dos**

- Set-up back-end user time tracking and sessions
  - Set-up script to seed db with data
  - Set-up queries for db
  - Collect user name on completion by creating a username component that appears on guess correct
  - Add a timeout for the game? Use client side counter to limit game time.
- Style the UI
  - Add a scoreboard at the bottom, fetch user scores upon guessing correctly
  - Change user feedback on guess location to pop-up on bottom of page instead of alert
